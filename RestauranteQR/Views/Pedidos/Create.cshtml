@model RestauranteQR.ViewModel.VMPedido

@{
    ViewData["Title"] = "Carta";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Tu Pedido</h1>

<p>
    <h2>HACE TU PEDIDO TOCANDO LOS BOTONES</h2>
</p>
<table class="table">
    @*<thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th></th>
        </tr>
    </thead>*@
    <tbody>
        @for (int i = 0; i < Model.ListaVMPeidoItem.Count; i++)
        {

        <tr>
            <td>
                @Html.DisplayFor(modelItem => Model.ListaVMPeidoItem[i].NombrePlato)
            </td>
            <td>
                <input type="hidden" asp-for="@Model.ListaVMPeidoItem[i].IdPlato" />
                
            </td>
            <td><button onclick="Agregar(@i)">+</button></td>
            <td>
                @Html.TextBoxFor(m => Model.ListaVMPeidoItem[i].Cantidad)
            </td>
            <td><button onclick="Agregar(@i)">-</button></td>

        </tr>
        }
    </tbody>
</table>
<script>

    function Agregar(indiceTocado) {
        var valor = document.getElementById('ListaVMPeidoItem_' + indiceTocado + '__Cantidad');
        alert(valor.value);

    }

</script>
